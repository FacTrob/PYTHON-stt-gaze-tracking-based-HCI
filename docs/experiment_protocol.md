# Experiment Protocol (A/B)

## Goal
- STT + gaze 결합이 단일 입력보다 작업 성공률/시간을 개선하는지 비교. # go to _lab\order.txt

## Conditions
C1 mouse/keyboard
C2 STT-only
C3 gaze-only(dwell)
C4 multimodal

## Tasks
A: grid target selection
B: file selection + action(open/delete)

## Metrics
- completion time (s)
- success rate
- error type counts (wrong target, wrong file, wrong action)
- latency (optional: STT listen->text)
